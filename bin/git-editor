#!/bin/bash

if [[ "$TERM_PROGRAM" == "idea" ]]; then
    idea --wait "$@"
elif [[ "$TERM_PROGRAM" == "vscode" ]]; then
    if [[ "$VSCODE_VARIANT" == "cursor" ]]; then
        cursor --wait "$@"
    else
        code --wait "$@"
    fi
else
    nvim -u NORC "$@"
fi